import Loader from '@/components/ui/loader';

const LoadingPage = () => {
  return (
    <main className='h-screen flex-1 w-full flex justify-center item-center'>
      <Loader />
    </main>
  );
};

export default LoadingPage;
