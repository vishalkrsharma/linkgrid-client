import { BeatLoader } from 'react-spinners';

const LoadingPage = () => {
  return (
    <main className='h-full flex-1 w-full flex justify-center item-center'>
      <BeatLoader />
    </main>
  );
};

export default LoadingPage;
